#!/usr/bin/env python

import numpy

import matplotlib.pyplot as plt

def read_gauge(path, time_interval=[0.0,1e10]):

    data = numpy.loadtxt(path, skiprows=2)


def plot_gauge_data():
    fig = plt.figure()

def plot_gauge_locations():
    fig = plt.figure()